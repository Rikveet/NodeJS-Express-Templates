{
  "name": "NodeJS-Express-Templates",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install && npm test && cd.. && cd frontend && npm install && npm test && npm run build && cd.. && cd backend && rm -r build || rmdir /s/q build && cd.. && cd frontend && mv build ../backend/ || move build ../backend/"
  }
}
