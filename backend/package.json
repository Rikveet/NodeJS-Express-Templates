{
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "nodemon": "^2.0.20"
  },
  "scripts": {
    "start": "nodemon --ignore client/* index.js",
    "build": "cd client && npm run build",
    "test": ""
  }
}
